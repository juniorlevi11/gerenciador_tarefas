@page "{id:int}"
@model GerenciadorTarefas.Pages.Tasks.EditModel

<h1>Editar Tarefa</h1>

<form method="post">
    <input type="hidden" asp-for="TaskItem.Id" /> <!-- Campo oculto para o ID -->
    <div>
        <label for="Title">Título</label>
        <input type="text" id="Title" name="TaskItem.Title" value="@Model.TaskItem.Title" required />
    </div>
    <div>
        <label for="Description">Descrição</label>
        <textarea id="Description" name="TaskItem.Description">@Model.TaskItem.Description</textarea>
    </div>
    <div>
        <label for="DueDate">Data de Vencimento</label>
        <input type="date" id="DueDate" name="TaskItem.DueDate" value="@Model.TaskItem.DueDate.ToString("yyyy-MM-dd")" required />
    </div>
    <div>
        <label for="IsCompleted">Concluído</label>
        <input type="hidden" name="TaskItem.IsCompleted" value="false" /> <!-- Campo oculto para enviar false -->
        <input type="checkbox" id="IsCompleted" name="TaskItem.IsCompleted" value="true" @(Model.TaskItem.IsCompleted ? "checked" : "") />
    </div>
    <button type="submit" class="btn btn-primary">Salvar Alterações</button>
    <a asp-page="/Tasks/ViewAll" class="btn btn-secondary">Cancelar</a>
</form>